source 'https://rubygems.org'

gemspec

# Pinned to the Rails version the consuming application runs, so that the rake
# task specs exercise the integration that actually ships.
gem 'railties', '8.0.5.1'
